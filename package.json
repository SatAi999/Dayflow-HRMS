{
  "name": "dayflow-hrms",
  "version": "1.0.0",
  "description": "Dayflow Human Resource Management System - Monorepo Scaffolding",
  "scripts": {
    "bootstrap": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "node scripts/run-all.js",
    "dev-backend": "cd backend && npm run dev",
    "dev-frontend": "cd frontend && npm run dev",
    "start-all": "node scripts/run-all.js",
    "build": "cd frontend && npm run build",
    "build-frontend": "cd frontend && npm run build",
    "lint": "cd frontend && npm run lint"
  }
}
